<%= simple_form_for([recipe, dose], remote: true, method: 'post') do |f| %>
  <%= f.input :description, label: false, placeholder: "Description" %>
  <%= f.association :ingredient,label: false %>
  <%= f.submit "Add Doses", class: "btn btn-primary" %>
<% end %>
